# Imports tool for drawing the rectangle
import turtle

# Gives length for sides of squares
s = 100
# Draws the square using the provided length
x = y = "iiii"
for x in x:
    for x in y:
        turtle.forward(s)
        turtle.right(90)
    turtle.right(90)
turtle.done()
